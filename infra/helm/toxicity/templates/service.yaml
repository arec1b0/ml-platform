apiVersion: v1
kind: Service
metadata:
  name: toxicity
spec:
  selector:
    app: toxicity
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: toxicity-canary     # Argo Rollouts требует два Service
spec:
  selector:
    app: toxicity
  ports:
    - port: 80
      targetPort: 8080